# Motor Tributário Conect 2026-2033
# Dependências Python

# IA — Extração de PDFs (extrator_pdfs.py)
anthropic>=0.40.0

# Validação de dados
pydantic>=2.0.0

# Testes
pytest>=7.0.0

# PDF parsing (fallback texto, quando não é imagem)
pdfplumber>=0.10.0

# Carregamento de .env (ANTHROPIC_API_KEY seguro — nunca hardcoded)
python-dotenv>=1.0.0
